services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ucdtt_rating
      POSTGRES_USER: ucdtt
      POSTGRES_PASSWORD: ucdtt
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: